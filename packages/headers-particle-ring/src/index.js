/**
 * @aura/headers-particle-ring — Lite + the WebGL "particleRing" background.
 * Registers the `particleRing` background; all Canvas2D backgrounds remain available.
 */
import '@aura/headers-core/backgrounds/particleRing'
export * from '@aura/headers-core'

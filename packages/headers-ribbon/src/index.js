/**
 * @aura/headers-ribbon — Lite + the WebGL "ribbon" background.
 * Registers the `ribbon` background; all Canvas2D backgrounds remain available.
 */
import '@aura/headers-core/backgrounds/ribbon'
export * from '@aura/headers-core'

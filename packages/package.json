{
  "name": "@aura/headers-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for the Aura animated-header React packages.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./headers-* build",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "@react-three/fiber": "^9.5.0",
    "@react-three/postprocessing": "^3.0.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.182.0",
    "tsup": "^8.5.0",
    "typescript": "^5.6.0"
  }
}

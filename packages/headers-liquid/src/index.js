/**
 * @aura/headers-liquid — Lite + the WebGL "liquid" mesh-gradient background.
 * Registers the `liquid` background; all Canvas2D backgrounds remain available.
 */
import '@aura/headers-core/backgrounds/liquid'
export * from '@aura/headers-core'
